{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "files.exclude": {
    "**/node_modules": true
  }
}
// Extensions I recommend:

// Prettier — Code formatter

// ESLint — linting rules

// GitLens — better git UI

// Code Runner — run single files quickly

// Bracket Pair Colorizer (or built-in)

// Jest Runner (if you use Jest)